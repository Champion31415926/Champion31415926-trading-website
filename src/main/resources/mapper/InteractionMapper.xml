<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mrblackpineapple.trading_website.mapper.InteractionMapper">

    <resultMap id="BaseResultMap" type="cn.mrblackpineapple.trading_website.vo.InteractionVO">
        <id column="id" property="id"/>
        <result column="userid" property="userId"/>
        <result column="productid" property="productId"/>
        <result column="username" property="userName"/>
        <result column="useraccount" property="userAccount"/>
        <result column="producttitle" property="productTitle"/>
        <result column="type" property="type"/>
        <result column="createtime" property="createTime"/>
    </resultMap>

    <insert id="save">
        INSERT INTO interaction (userid, productid, type, createtime)
        VALUES (#{userId}, #{productId}, #{type}, #{createTime})
    </insert>

    <delete id="batchDelete" parameterType="list">
        DELETE FROM interaction WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="query" resultMap="BaseResultMap">
        SELECT i.*,
        u.username,
        u.useraccount,
        p.name AS producttitle
        FROM interaction i
        LEFT JOIN "user" u ON u.id = i.userid
        LEFT JOIN product p ON p.id = i.productid
        <where>
            <if test="id != null">
                AND i.id = #{id}
            </if>
            <if test="userId != null">
                AND i.userid = #{userId}
            </if>
            <if test="productId != null">
                AND i.productid = #{productId}
            </if>
            <if test="type != null">
                AND i.type = #{type}
            </if>
            <if test="startTime != null and endTime != null">
                AND i.createtime BETWEEN #{startTime} AND #{endTime}
            </if>
        </where>
        ORDER BY i.id DESC
        <if test="current != null and size != null">
            LIMIT #{size} OFFSET #{current}
        </if>
    </select>

    <select id="queryCount" resultType="integer">
        SELECT COUNT(*)
        FROM interaction i
        <where>
            <if test="id != null">
                AND i.id = #{id}
            </if>
            <if test="userId != null">
                AND i.userid = #{userId}
            </if>
            <if test="productId != null">
                AND i.productid = #{productId}
            </if>
            <if test="type != null">
                AND i.type = #{type}
            </if>
            <if test="startTime != null and endTime != null">
                AND i.createtime BETWEEN #{startTime} AND #{endTime}
            </if>
        </where>
    </select>

    <select id="queryByProductIds" resultMap="BaseResultMap">
        SELECT i.*
        FROM interaction i
        WHERE i.productid IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>
